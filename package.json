{
  "name": "monarchliquor.com",
  "private": true,
  "type": "module",
  "description": "Static site for Monarch Liquor, built with Eleventy and deployed to GitHub Pages.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "images": "node scripts/optimize-images.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "test": "node --test lib/**/*.test.js",
    "check": "npm run build && npm run check:links && npm run check:seo && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.33.5"
  }
}
